[tool.poetry]
name = "katas"
version = "0.1.0"
description = "Train your algo skillz"
authors = ["James Raubenheimer"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.12"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
